# PostgreSQL connection for OrgSight
# Copy to POSTGRES/.env and fill in real values.
#
# PGDATABASE must be exactly OrgSight_db (enforced by pg_adapter.py).

PGHOST=your-postgres-host.example.com
PGPORT=5432
PGDATABASE=OrgSight_db
PGUSER=your_db_user
PGPASSWORD=your_db_password
PGSSLMODE=require
