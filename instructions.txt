org chart for census data for HR data
(who reports to whom - employee specific)

- x marked as red - cost updated

- last changes stored

- summary to be provided

- export in ppt ( export updated chart)

---
DATABASE & CLI (full command list)
See DB_COMMANDS.md for every CRUD command (users, projects, assignments, datasets, scenarios, audit, tokens, locks).

Quick start (repo root):
  python manage_db.py tables
  python manage_db.py users list
  python manage_db.py users set-password <username> <password>
  python manage_db.py projects list
  python manage_db.py assignments assign --project 1 --user <username>

DB file: org_lvl_analysis_backend/org_lvl_analysis_backend/db/orgsight.db
Dev passwords file: .../db/credentials.local.json (gitignored)

---
SERVERS (use these ports only)
  Backend:  http://127.0.0.1:8001   ->  .\scripts\start-backend.ps1
  Frontend: http://localhost:5173    ->  .\scripts\start-frontend.ps1
  Org chart: http://localhost:5174 ->  .\scripts\start-org-chart.ps1
  (Port 8000 is NOT used by the app.)